name: Hourly Goal metrics (VM managed)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hourly Goal metrics are managed by the authorized reader VM at minute 59."
